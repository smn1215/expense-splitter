# Expense Splitter

An AI-assisted full-stack application for managing group expenses.

## Project Setup

### Frontend Setup
To run the frontend client, open `frontend/index.html` in any browser or serve it using standard `npm`:
```bash
npm run dev
